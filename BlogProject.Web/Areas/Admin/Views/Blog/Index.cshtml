@model List<BlogListModel>

<a href="" class="btn btn-info">New Blog</a>
<table class="table table-bordered table-hover">
    <tbody>
    @foreach (var blog in Model)
    {
        <tr>
            <td>@blog.Title</td>
            <td>
                <a href="">Edit</a> 
                <a href="">Delete</a>
            </td>
        </tr>
    }
    </tbody>

</table>